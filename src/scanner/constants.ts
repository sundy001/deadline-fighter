import { join } from "path";

export const OCR_SPACE_API_KEY = "GP8843VHX778X";
export const CACHE_PATH = join(process.cwd(), ".cache");
export const HEIC_PATH = join(process.cwd(), "heic");
export const REPORT_PATH = join(process.cwd(), "report.json");
